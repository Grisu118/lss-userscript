{
  "name": "mission-vehicle-filter",
  "private": true,
  "version": "1.1.1",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "config": "tsc --showConfig"
  }
}
